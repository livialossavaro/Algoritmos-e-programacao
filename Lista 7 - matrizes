#include <stdio.h>

int main () {

//Exercicio 1
	
	int matriz[4][3], i, j;
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 3; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 3; j++) {

 	printf("%i\t", matriz[i][j]);
 	
	}
	printf("\n");
 }
	
//Exercicio 2

int matriz[4][3], i, j, soma;
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 4; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 4; j++) {

 	printf("%i\t", matriz[i][j]);
 	soma += matriz[i][j];
 	
	}
	printf("\n");
 }
	printf("\n
	Soma: %i", soma);

//Exercicio 3

int matriz[5][3], i, j, num, posicaoi, posicaoj;
 
 for (i = 0; i < 5; i++) {
 	for (j = 0; j < 3; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 5; i++) {
 	for (j = 0; j < 3; j++) {
 		
 		if(num < matriz[i][j]) {
 			num = matriz[i][j];
            posicaoi = i;
            posicaoj = j;
		 }

 	printf("%i\t", matriz[i][j]);
 	
	}
	printf("\n");
 }
	
	printf("\nMaior numero: %i", num);
	printf("\n\nPosicao:\n\nLinha: %i\nColuna: %i", posicaoi, posicaoj);

//Exercicio 4

 int matriz[3][3], i, j;
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 3; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 3; j++) {
 		printf("%i\t", matriz[i][j]);
	 }
	printf("\n");
 }
 
 printf("\n");
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 3; j++) {
 		printf("%i\t", matriz[i][j] * 2);
	 }
	printf("\n");
 }

//Exercicio 5

int matriz[4][4], i, j, num, soma, vetor[4];
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 4; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 4; j++) {
 		printf("%i\t", matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 4; j++) {
 		if (i == j) {
 			soma += matriz[i][j];
 			vetor[i] = matriz[i][j];
	    }
    }
 }
 
 printf("\n");
 
 for (j = 0; j < 4; j++) {
        printf("%i ", vetor[j]);
    }
    
 printf("\n\nSoma diagonal princial: %i", soma);

//Exercicio 6

int main () {
	
 int matriz[3][4], i, j, linhas[3], linha;
 float media;
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 4; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");                                                                  
 }
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 4; j++) {
 		printf("%i\t", matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 3; i++) {
        linhas[i] = 0;

        for (j = 0; j < 4; j++) {
            linhas[i] += matriz[i][j];
        }
	printf("\nSoma linha %i: %i",i, linhas[i]);
   
    }
    
 for (i = 0; i < 3; i++) {
 	
  printf("\n\nEscolha uma linha para calcular a media dos numeros: ");
  scanf("%i", &linha);  
  
  media = (float)linhas[linha] / 4;  
  printf("Media: %.2f", media);
 } 

//Exercicio 7

 int matriz[3][5], i, j, colunas[5];
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 5; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 5; j++) {
 		printf("%i\t", matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 5; j++) {
 		if ( matriz[i][j] < 0) {
 			matriz[i][j] = 0;
		 }
	 }
	printf("\n");
 }
 
 for (i = 0; i < 3; i++) {
 	for (j = 0; j < 5; j++) {
 		printf("%i\t", matriz[i][j]);
	 }
	printf("\n");
 }
 
 
 	for (j = 0; j < 5; j++) {
 		colunas[j] = 0;
	 
	for (i = 0; i < 3; i++) {
 		colunas[j] += matriz[i][j];
	 }
	 }
	 
 	for (j = 0; j < 5; j++) {
 		printf("\nColuna %i: %i", j,colunas[j]);
 	}
	 
 //Exercicio 8

int matriz[4][4],i, j, num, linhamaiornum, colunamaiornum, menor;
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 4; j++) {
 		
 		printf("Numero [%i][%i]: ", i, j);
 		scanf("%i", &matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 4; j++) {
 		printf("%i\t", matriz[i][j]);
	 }
	printf("\n");
 }
 
 for (i = 0; i < 4; i++) {
 	for (j = 0; j < 4; j++) {
 		if (num < matriz[i][j]) {
 			num = matriz[i][j];
 			linhamaiornum = i;
 			colunamaiornum = j;
 			
		 }
	 }
 }
 
  menor = matriz[linhamaiornum][0];
  
  for (j = 1; j < 4; j++) {
        if (matriz[linhamaiornum][j] < menor) {
            menor = matriz[linhamaiornum][j];
        }
    }

    printf("\nMaior elemento: %d | linha %d, coluna %d |", num, linhamaiornum, colunamaiornum);
    printf("\nElemento minimax: %d | linha %d)", menor, linhamaiornum);

	return 0;
}
